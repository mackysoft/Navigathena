namespace MackySoft.Navigathena
{
    /// <summary>The histories whose presentation can be affected by an effect, not its ownership lifetime.</summary>
    public enum NavigationTransitionScope
    {
        Region,
        Host,
    }
}
