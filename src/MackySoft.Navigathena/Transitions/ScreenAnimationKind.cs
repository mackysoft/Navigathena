namespace MackySoft.Navigathena
{
    /// <summary>The presentation change of one screen, independent of its activity and input permission.</summary>
    public enum ScreenAnimationKind
    {
        Enter,
        Cover,
        Reveal,
        Exit
    }
}
